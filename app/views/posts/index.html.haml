- if @posts.blank?
  = render :partial => "posts/no_posts"

- else
  %h1.listing= page_entries_info(@posts)
  - @posts.each do |p|
    = render :partial => "posts/in_listing", :locals => {:post => p}
  = will_paginate @posts
  .clear